{
  "schema": 1,
  "name": "telegram",
  "title": "Telegram",
  "icon_name": "Telegram",
  "description": "A Telegram bot account over the Bot API \u2014 the bot's chats become conversations, and it really sends.",
  "kind": "datasource.api.telegram",
  "auth": {
    "secrets": {
      "bot_token": ""
    }
  },
  "config": {
    "bot_token": {
      "type": "text",
      "required": true,
      "label": "Bot token",
      "placeholder": "123456:ABC-...",
      "hint": "From @BotFather (/newbot). The token is the bot's credential; the visible identity (@username) is stamped from getMe on first sync \u2014 deliberately NOT account_key, which would display the secret."
    },
    "base_url": {
      "type": "text",
      "advanced": true,
      "label": "API base URL"
    }
  }
}

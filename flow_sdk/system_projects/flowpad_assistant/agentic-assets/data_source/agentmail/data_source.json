{
  "schema": 1,
  "name": "agentmail",
  "title": "AgentMail",
  "icon_name": "Mail",
  "description": "A hosted mailbox over its HTTP API — reads the inbox and really sends.",
  "config": {
    "inbox": {
      "type": "text",
      "required": true,
      "label": "Inbox",
      "placeholder": "you@agentmail.to",
      "account_key": true
    },
    "api_key": {
      "type": "text",
      "required": true,
      "label": "API key",
      "placeholder": "am_...",
      "hint": "The AgentMail API key this inbox was created under. The driver reads config.api_key — the old auth.env declaration was consumed by nothing on a builtin driver, so a form-created source could never authenticate."
    },
    "base_url": {
      "type": "text",
      "advanced": true,
      "label": "API base URL"
    }
  }
}

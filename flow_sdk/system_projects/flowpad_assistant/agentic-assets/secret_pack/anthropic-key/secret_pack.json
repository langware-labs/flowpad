{
  "schema": 2,
  "name": "anthropic-key",
  "title": "Anthropic API key",
  "icon_name": "ClaudeCode",
  "description": "An Anthropic API key, billed to your own account. Separate from signing in to Claude, which spends a subscription instead.",
  "setup": "Get an Anthropic API key for this machine.\n1. Open https://console.anthropic.com/settings/keys and sign in (ask the person to sign in if a login is needed — never create an account).\n2. Create a key named \"Flowpad\" and copy it (it starts with `sk-ant-`).\n3. Store it: `flow credentials set anthropic-key ANTHROPIC_API_KEY=<key>`.\n4. Confirm with `flow credentials check anthropic-key`. Never print or repeat the key.",
  "help_url": "https://console.anthropic.com/settings/keys",
  "lm_provider": "anthropic",
  "vars": {
    "ANTHROPIC_API_KEY": {
      "label": "API key",
      "placeholder": "sk-ant-…",
      "hint": "Console → Settings → API keys. A workspace-scoped key works.",
      "help_url": "https://console.anthropic.com/settings/keys",
      "secret": true,
      "required": true
    }
  }
}

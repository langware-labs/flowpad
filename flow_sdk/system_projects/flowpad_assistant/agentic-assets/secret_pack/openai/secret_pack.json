{
  "schema": 2,
  "name": "openai",
  "title": "OpenAI",
  "icon_name": "Codex",
  "description": "An OpenAI API key, billed to your own account.",
  "setup": "Get an OpenAI API key for this machine.\n1. Open https://platform.openai.com/api-keys and sign in (ask the person to sign in if needed — never create an account).\n2. Create a secret key named \"Flowpad\" and copy it (it starts with `sk-`).\n3. Store it: `flow credentials set openai OPENAI_API_KEY=<key>`.\n4. Confirm with `flow credentials check openai`. Never print or repeat the key.",
  "help_url": "https://platform.openai.com/api-keys",
  "lm_provider": "openai",
  "vars": {
    "OPENAI_API_KEY": {
      "label": "API key",
      "placeholder": "sk-…",
      "hint": "Platform → API keys. A project key works.",
      "help_url": "https://platform.openai.com/api-keys",
      "secret": true,
      "required": true
    }
  }
}

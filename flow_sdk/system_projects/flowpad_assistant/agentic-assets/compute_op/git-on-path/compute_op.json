{
  "type": "compute_op",
  "id": "611f60c4-037e-48a2-9fc1-3948ef773652",
  "name": "git-on-path",
  "label": "Git",
  "description": "git is on the PATH and runnable.",
  "completion_check": {
    "commands": {
      "darwin": "git --version >/dev/null 2>&1",
      "linux": "git --version >/dev/null 2>&1",
      "win32": "git --version *> $null; if ($LASTEXITCODE -eq 0) { exit 0 } else { exit 1 }"
    },
    "timeout_seconds": 30
  },
  "attempts": [
    {
      "kind": "command",
      "commands": {
        "darwin": "brew install git",
        "linux": "apt-get update && apt-get install -y git",
        "win32": "winget install --id Git.Git --silent"
      },
      "timeout_seconds": 600
    },
    {
      "kind": "agent",
      "agent": "capability-installer",
      "prompt": "Capability to install: Git. Official page with install instructions: https://git-scm.com/downloads\n\nInstall Git on this machine so that `git --version` succeeds from a fresh non-login shell. Assume a bare machine. Prefer the platform's official package manager (apt-get/dnf/apk on Linux, Homebrew or the Xcode command line tools on macOS, winget on Windows). Do NOT configure user.name or user.email, do NOT set up credentials, and do NOT clone anything. Report the version you installed and stop.",
      "name": "Install Git",
      "timeout_seconds": 1800
    }
  ]
}

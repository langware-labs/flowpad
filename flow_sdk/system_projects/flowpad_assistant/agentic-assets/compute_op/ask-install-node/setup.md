Asks before installing Node.js. Send means yes; Cancel means leave it uninstalled.

The check prints `{}` when the tool is already there: an ask op's check prints the value it returns, and a confirm has no fields.

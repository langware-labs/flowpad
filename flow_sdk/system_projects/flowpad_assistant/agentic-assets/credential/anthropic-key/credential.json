{
  "schema": 1,
  "name": "anthropic-key",
  "title": "Anthropic API key",
  "icon_name": "ClaudeCode",
  "description": "An Anthropic API key, billed to your own account. Separate from signing in to Claude, which spends a subscription instead.",
  "help_url": "https://console.anthropic.com/settings/keys",
  "lm_provider": "anthropic",
  "vars": {
    "ANTHROPIC_API_KEY": {
      "label": "API key",
      "placeholder": "sk-ant-…",
      "hint": "Console → Settings → API keys. A workspace-scoped key works.",
      "help_url": "https://console.anthropic.com/settings/keys",
      "secret": true,
      "required": true
    }
  }
}

"""The shipped data sources, one package per provider, each a ``flow_sdk.sources`` class.

A provider package imports the contract and the standard library only — the same rule an
authored ``source.py`` in a ``data_source`` asset lives under, because it is the same kind of
thing.
"""

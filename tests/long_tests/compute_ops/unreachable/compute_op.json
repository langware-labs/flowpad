{
  "type": "compute_op",
  "name": "unreachable",
  "label": "an unreachable goal",
  "description": "A goal whose check never passes — the negative case.",
  "subkind": "cli",
  "exe_data": {
    "commands": {
      "linux": "true"
    }
  },
  "completion_check": {
    "commands": {
      "linux": "exit 1"
    }
  }
}

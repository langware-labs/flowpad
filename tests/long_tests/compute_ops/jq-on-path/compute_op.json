{
  "type": "compute_op",
  "name": "jq-on-path",
  "label": "jq",
  "description": "jq is on the PATH.",
  "completion_check": {
    "commands": {
      "linux": "command -v jq"
    }
  },
  "attempts": [
    {
      "kind": "command",
      "commands": {
        "linux": "apt-get update && apt-get install -y jq"
      }
    }
  ]
}

/**
 * Re-export of the SDK hook — see `@sdk/react/hooks/useEntityData`.
 *
 * The UI once carried its own copy; the two differed only in an import path.
 */
export { useEntityData } from '@sdk/react/hooks/useEntityData';
export type { UseEntityDataResult } from '@sdk/react/hooks/useEntityData';

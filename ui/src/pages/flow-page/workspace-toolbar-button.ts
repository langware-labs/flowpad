/**
 * The vibe workspace toolbar's icon-button look — one visual contract shared by
 * the affordances sitting in that row (Collaborate, Assign), so they can't drift
 * apart pixel by pixel.
 */
export const workspaceToolbarButton =
  'flex h-6 w-6 items-center justify-center rounded text-muted-foreground hover:bg-muted hover:text-foreground disabled:cursor-not-allowed disabled:opacity-40';

// Compatibility export for the page-local name. The control is shared with
// transcript recovery notices, so its implementation lives with worker UI.
export {
  WorkerTypeSelect as VibeWorkerSelect,
  type WorkerTypeSelectProps as VibeWorkerSelectProps,
} from '@src/components/workers/WorkerTypeSelect';

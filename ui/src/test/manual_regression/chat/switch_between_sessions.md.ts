import { test } from '@playwright/test';

test.describe('Switch Between Sessions', () => {
  test.skip('legacy session tab UI was removed; active sessions now switch through terminal tabs', async () => {});
});

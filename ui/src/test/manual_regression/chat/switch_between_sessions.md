test 1: Create multiple session tabs and switch between them
- navigate to {APP_URL}/
- fill landing input with "session switch test"
- press Enter
- wait for URL to change to /dock/session/...
- click "New Session" button
- validate Session 1 tab is visible
- validate instruction input is visible and status is IDLE
- fill instruction input with "I am session one"
- press Enter
- wait for DONE status
- validate user message and AI response appear
- note the session URL as Session A URL
- click "+" button to add a new session tab
- validate a second session tab appears
- validate the new session shows "no output"
- validate status is IDLE
- note the session URL as Session B URL (different from Session A)
- validate Session A URL differs from Session B URL

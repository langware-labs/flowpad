/**
 * Rule authoring pieces, used by the merged Events screen
 * (`@src/components/events`). The screen body and its activity feed live there;
 * what remains here is the rules navigator and the per-match-mode editors.
 */
export { TriggersNavigator } from './TriggersNavigator';

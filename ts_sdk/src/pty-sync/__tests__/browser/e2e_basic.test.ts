import { describe, it } from 'vitest';

// Browser E2E tests — require Playwright + real xterm.js
// Implemented when Playwright integration is added.

describe('Browser E2E — basic replay', () => {
  it.todo('PlaybackHarness + LiveXtermAdapter replay: validateBuffer() passes');
  it.todo('StubXtermAdapter inject: all live rows match predicted content');
});
